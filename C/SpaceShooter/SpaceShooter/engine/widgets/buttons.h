#pragma once
#include "../base.h"


// Returns true if mouse was pressed in the zone, set make_visible = false, if you want to create a custom button.
bool CreateButton(SDL_Rect shape, bool make_visible);
